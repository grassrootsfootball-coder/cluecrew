export default function HomePage() {
  return (
    <main style={{ padding: '4rem 2rem', maxWidth: 640, margin: '0 auto' }}>
      <img src="/cluecrew-logo.svg" alt="ClueCrew" width={320} height={81} />
      <p>ClueCrew makes the 11+ make sense — for every child and every parent.</p>
      <p>The foundation build is underway. Nothing to see here yet.</p>
    </main>
  );
}
