# dependencies
node_modules/

# next.js
.next/
out/

# builds
dist/
*.tsbuildinfo

# env — secrets never live in the repo (BUILD-PHASE-1 §2)
.env
.env.*
!.env.example

# testing
coverage/
playwright-report/
test-results/
blob-report/

# misc
.DS_Store
.vercel

# Lighthouse CI report artifacts (pnpm perf)
.lighthouseci/
.env*

# Generated reviewer packs and returned decisions files (pnpm export:review-pack).
# Output of the repo, not input to it; regenerate rather than commit.
content/exports/
